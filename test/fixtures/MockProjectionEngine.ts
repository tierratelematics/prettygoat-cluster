import {IProjectionEngine, PushContext, IProjection} from "prettygoat";

export default class MockProjectionEngine implements IProjectionEngine {
    run(projection?: IProjection<any>, context?: PushContext) {

    }
}